<form method="post" action="{{ path('app_reservationservice_delete', {'id': reservationservice.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservationservice.id) }}">
    <button class="btn btn-danger" style="width:80px">Delete</button>

       
</form>

