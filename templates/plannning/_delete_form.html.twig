<form method="post" action="{{ path('app_plannning_delete', {'idplan': planning.idplan}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planning.idplan) }}">
	<button class="btn btn-outline-danger">Delete</button>
</form>
