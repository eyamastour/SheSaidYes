<form method="post" action="{{ path('Admin_reclamation_delete_reponse', {'idreclamation': reclamation.idreclamation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.idreclamation) }}">
    <button class="btn btn-danger">Delete</button>
</form>